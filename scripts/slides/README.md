# Slide Generators

Canonical local slide-generation subsystem. Use `INDEX.yaml`, `common/slide_defaults.py`, or `scripts/bin/thesis-script list slides` to find generators. Outputs should default to `dataOutput/slide_assets/<campaign>/<slide_key>/`; Google Slides mutation is out of scope unless explicitly requested.
