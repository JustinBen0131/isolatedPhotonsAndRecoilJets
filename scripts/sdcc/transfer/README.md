# Transfer Helpers

Canonical source for local SFTP helpers.

These helpers are local-only. They should not be uploaded to SDCC. Keep the
top-level compatibility command paths available because policies and repeated
operator commands call `scripts/sftp_push_recoiljets.sh` and
`scripts/sftp_get_recoiljets_outputs.sh` directly.
