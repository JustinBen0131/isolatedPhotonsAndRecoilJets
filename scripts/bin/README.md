# Command Gateways

`thesis-script` is the stable resolver for indexed script IDs, basenames, old paths, and canonical paths. It can list, resolve, or run scripts without keeping every old name in the parent `scripts/` directory.
