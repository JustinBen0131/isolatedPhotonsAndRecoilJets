# Compatibility Aliases

Old local script names that are no longer hard parent contracts live here as symlinks to canonical source. Prefer `scripts/bin/thesis-script path <name>` or the canonical path in `scripts/COMMAND_INDEX.tsv` for new work.
