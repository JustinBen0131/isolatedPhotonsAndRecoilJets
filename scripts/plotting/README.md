# Plotting Helpers

Reusable plotting utilities organized by analysis lane: `auau_bdt/`, `pp_currentian/`, `stitching/`, `efficiency/`, `trigger/`, and `truth_purity/`. Slide builders belong in `scripts/slides/`.
