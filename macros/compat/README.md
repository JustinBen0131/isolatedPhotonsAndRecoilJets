# Macro Compatibility

`local/` contains lower-risk compatibility wrappers retired from the top-level `macros/` command surface.

Use `macros/bin/thesis-macro path <id-or-basename>` to resolve canonical implementations. Top-level wrappers remain only for active hard aliases documented in `MACRO_INDEX.yaml`.
