#!/usr/bin/env python3
"""Repository-local entry point that works before package installation."""

from pathlib import Path
import sys

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "python"))

from photonjet.cli import main

raise SystemExit(main())
